6)

SELECT EMPNO,ENAME,JOB_DESCR,SAL,DEPTNO
FROM EMP JOIN JOB ON EMP.JOBCODE=JOB.JOBCODE
WHERE JOB_DESCR = 'ΠΩΛΗΤΗΣ';

7)

SELECT MAX(SAL), MIN(SAL), AVG(SAL), SAL+ IFNULL(COMM,0), COUNT(COMM), COUNT(EMPNO) FROM EMP JOIN JOB ON EMP.JOBCODE=JOB.JOBCODE

8)

SELECT MAX(SAL),AVG(SAL) FROM EMP JOIN JOB ON EMP.JOBCODE=JOB.JOBCODE WHERE JOB_DESCR='ΑΝΑΛΥΤΗΣ';

9)

SELECT EMPNO,ENAME,JOB_DESCR,SAL,DEPTNO
 FROM EMP JOIN JOB ON EMP.JOBCODE=JOB.JOBCODE 
 WHERE JOB_DESCR='ΑΝΑΛΥΤΗΣ' AND SAL BETWEEN 1000 AND 2500;

10)

SELECT EMPNO,ENAME,JOB_DESCR,SAL,DEPTNO
 FROM EMP JOIN JOB ON EMP.JOBCODE=JOB.JOBCODE 
 WHERE ENAME LIKE '%D%';

11)

SELECT EMPNO,ENAME,JOB_DESCR,SAL,DEPTNO
 FROM EMP JOIN JOB ON EMP.JOBCODE=JOB.JOBCODE 
 ORDER BY DEPTNO AND SAL;

12)

 SELECT AVG(SAL),COUNT(*)
 FROM EMP JOIN JOB ON EMP.JOBCODE=JOB.JOBCODE
 GROUP BY DEPTNO;